# T10.6 BGE-M3 Embedding 验证

## 结论

- 输入 Chunk / Embedding：32 / 32。
- Schema 有效：32；唯一 Chunk ID：32。
- Dense：1024 维；Sparse 非空：32 条。
- 最大输入：753 tokens；截断：0 条。
- 结构性内容：0 条，全部保留标记。
- 重复编码检查：Dense 通过，Sparse 通过。
- 全部验收检查：通过。

## 固定配置

- 模型：`BAAI/bge-m3`
- revision：`5617a9f61b028005a4858fdac845db406aefb181`
- 实现：`FlagEmbedding.BGEM3FlagModel 1.4.0`
- 设备：`cuda:0`；推理精度：`float16`；保存精度：`float32`。
- max_length：`1024`；batch_size：`1`；ColBERT：关闭。
- 输入格式：`section_path + 两个换行 + text`；Metadata 标签不注入模型文本。

## 统计

- Token：最小 414，P50 594，P95 683，最大 753。
- Sparse NNZ：最小 117，P50 197，P95 256，最大 295。
- 每来源记录：`{'fine_pitch_csp_assembly_processes': 10, 'lead_free_reflow_defects': 8, 'solder_paste_printing_critical_parameters': 14}`
- GPU 峰值已分配显存：`1109.99` MB。
- Embedding 耗时：`2.992` 秒；总耗时：`16.558` 秒。

## 完整性检查

- 每条记录保存原始 text_hash、Embedding 输入哈希、模型 revision、配置版本、设备和精度。
- Dense 数值均有限且归一化；Sparse 索引排序且唯一，权重均为有限正数。
- pip check：`No broken requirements found.`
